wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash miniconda
export PATH=~/miniconda3/bin/:$PATH
source activate tensorflow


pip install tensorflow-gpu==1.12 
pip3 install --upgrade --force-reinstall tensorflow-gpu==1.10.0

pip install tensorflow==1.12
pip uninstall tensorflow
python
import tensorflow as tf
tf.__version__
exit()
pip install jupyter
jupyter notebook --ip=0.0.0.0 --port=1088 &

gpustat

打开训练log
路径 tensorboard --logdir . -port 82768

echo export PATH=~/miniconda3/bin:\$PATH >> ~/.bashrc
echo export PATH=/usr/local/cuda-9.0/bin\${PATH:+:\${PATH}} >> ~/.bashrc
echo export LD_LIBRARY_PATH=/usr/local/cuda-9.0/lib64\${LD_LIBRARY_PATH:+:\${LD_LIBRARY_PATH}} >> ~/.bashrc
source ~/.bashrc
eval(
复制环境

cd 
vim cnn.py 



1.代码风格乱，用统一的代码
2.边讲边写代码，让同学们跟着做
3.可以在本地跑，不一定非要在Linux，环境提前整理好，数据内容简化不做
4.先要把基础内容讲清楚，否则后面都跟不上
5.理论和实践结合把流程步骤讲清楚，先提供TensorFlow基础语法资料，cook平台上TensorFlow实战课程




